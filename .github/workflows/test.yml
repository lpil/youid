name: test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: erlef/setup-beam@v1
        with:
<<<<<<< HEAD
          otp-version: "27"
          gleam-version: "1.10.0"
=======
          otp-version: "27.3.0"
          gleam-version: "1.9.1"
>>>>>>> 6e9a3cd (Change from ffi to gleam_time, support js)
          rebar3-version: "3"
      - run: gleam deps download
      - run: gleam test
      - run: gleam format --check src test
